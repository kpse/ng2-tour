/* tslint:disable:no-unused-variable */

import {TestBed, async, inject} from '@angular/core/testing';
import {LogService} from './log-service.service';

describe('LogService', () => {
  beforeEach(() => {});

  it('should ...', () => {
    let service = new LogService(true);
    expect(service.log).toBeTruthy();
  });
});
